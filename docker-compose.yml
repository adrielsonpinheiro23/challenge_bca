services:
  tests:
    build: .
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
      - ./allure-results:/app/allure-results
      - ./test-results:/app/test-results
    command: npm test
